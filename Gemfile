$LOAD_PATH.unshift File.expand_path("../lib", __FILE__)
require 'test_gem'
require 'gemspec'

# TODO: Below, change the gem name, authors, and email

Gem::Specification.new do |s|
  s.name        = 'test_for_root'
  s.version     = TestForRoot::VERSION
  s.date        = '2013-02-09'
  s.summary     = "A gem that tests for root"
  s.description = "A gem that tests for root on *nix systems with a basic API"
  s.authors     = ["Peter Bomars"]
  s.email       = 'pbomars@gmail.com'
  s.homepage    = 'http://rubygems.org/gems/test_for_root'
  s.files       = [""]
  s.executables << 'test_for_root'
end